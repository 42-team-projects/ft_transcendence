[server]
protocol = https
http_addr =
http_port = 3000
domain = ${GF_SERVER_DOMAIN}
root_url = %(protocol)s://%(domain)s:%(http_port)s/
router_logging = false
static_root_path = public
enable_gzip = false
cert_file = /etc/grafana/server.cert
cert_key = /etc/grafana/server.key
